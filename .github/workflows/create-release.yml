name: Create Release

on:
  workflow_dispatch:
    inputs:
      version_type:
        description: 'Тип релиза'
        required: true
        default: 'auto'
        type: choice
        options:
          - auto      # Автоматически определить по коммитам
          - patch     # 0.0.1 → 0.0.2 (багфиксы)
          - minor     # 0.1.0 → 0.2.0 (новые фичи)
          - major     # 1.0.0 → 2.0.0 (breaking changes)
          - prerelease # 0.1.0 → 0.1.1-beta.1
      custom_version:
        description: 'Кастомная версия (опционально, например: 1.0.0-beta.1)'
        required: false
        type: string
      release_notes:
        description: 'Заметки к релизу (опционально)'
        required: false
        type: string

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Нужно для анализа коммитов
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
          
      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
      - name: Determine new version
        id: version
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          
          # Если указана кастомная версия
          if [ -n "${{ github.event.inputs.custom_version }}" ]; then
            NEW_VERSION="${{ github.event.inputs.custom_version }}"
            echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
            echo "version_type=custom" >> $GITHUB_OUTPUT
            echo "🎯 Используем кастомную версию: $NEW_VERSION"
            exit 0
          fi
          
          VERSION_TYPE="${{ github.event.inputs.version_type }}"
          
          # Автоматическое определение типа релиза
          if [ "$VERSION_TYPE" = "auto" ]; then
            echo "🔍 Анализируем коммиты для определения типа релиза..."
            
            # Получаем коммиты с последнего тега
            LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
            if [ -n "$LAST_TAG" ]; then
              COMMITS=$(git log $LAST_TAG..HEAD --oneline)
              echo "Коммиты с $LAST_TAG:"
              echo "$COMMITS"
            else
              COMMITS=$(git log --oneline)
              echo "Все коммиты (нет предыдущих тегов):"
              echo "$COMMITS"
            fi
            
            # Определяем тип релиза по conventional commits
            if echo "$COMMITS" | grep -qE "^[a-f0-9]+ [^:]+!:"; then
              VERSION_TYPE="major"
              echo "🚨 Найдены breaking changes → MAJOR релиз"
            elif echo "$COMMITS" | grep -qE "^[a-f0-9]+ feat(\(.+\))?:"; then
              VERSION_TYPE="minor"
              echo "✨ Найдены новые фичи → MINOR релиз"
            elif echo "$COMMITS" | grep -qE "^[a-f0-9]+ fix(\(.+\))?:"; then
              VERSION_TYPE="patch"
              echo "🐛 Найдены исправления → PATCH релиз"
            else
              VERSION_TYPE="patch"
              echo "📝 Другие изменения → PATCH релиз (по умолчанию)"
            fi
          fi
          
          echo "version_type=$VERSION_TYPE" >> $GITHUB_OUTPUT
          
          # Вычисляем новую версию
          IFS='.' read -r major minor patch <<< "$CURRENT_VERSION"
          
          # Убираем возможные суффиксы из patch версии
          patch=$(echo "$patch" | sed 's/-.*//')
          
          case $VERSION_TYPE in
            major)
              NEW_VERSION="$((major + 1)).0.0"
              ;;
            minor)
              NEW_VERSION="$major.$((minor + 1)).0"
              ;;
            patch)
              NEW_VERSION="$major.$minor.$((patch + 1))"
              ;;
            prerelease)
              # Для prerelease добавляем суффикс
              if [[ "$CURRENT_VERSION" == *"-"* ]]; then
                # Уже есть суффикс, увеличиваем номер
                BASE_VERSION=$(echo "$CURRENT_VERSION" | cut -d'-' -f1)
                SUFFIX=$(echo "$CURRENT_VERSION" | cut -d'-' -f2)
                if [[ "$SUFFIX" == *"."* ]]; then
                  PREFIX=$(echo "$SUFFIX" | cut -d'.' -f1)
                  NUMBER=$(echo "$SUFFIX" | cut -d'.' -f2)
                  NEW_VERSION="$BASE_VERSION-$PREFIX.$((NUMBER + 1))"
                else
                  NEW_VERSION="$BASE_VERSION-$SUFFIX.1"
                fi
              else
                NEW_VERSION="$major.$minor.$((patch + 1))-beta.1"
              fi
              ;;
          esac
          
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "🎉 Новая версия: $CURRENT_VERSION → $NEW_VERSION ($VERSION_TYPE)"

      - name: Create and push tag
        run: |
          TAG_NAME="v${{ steps.version.outputs.new_version }}"
          echo "🏷️ Создаем тег: $TAG_NAME"
          
          # Создаем аннотированный тег
          git tag -a "$TAG_NAME" -m "Release $TAG_NAME"
          
          # Пушим тег
          git push origin "$TAG_NAME"
          
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          
      - name: Create release branch
        run: |
          BRANCH_NAME="release/v${{ steps.version.outputs.new_version }}"
          echo "📝 Создаем ветку: $BRANCH_NAME"
          
          git checkout -b "$BRANCH_NAME"
          
          # Обновляем версии во всех пакетах
          echo "📦 Обновляем версии пакетов..."
          node ./scripts/update-package-versions.js ${{ steps.version.outputs.new_version }}
          
          # Создаем коммит с обновлением версий
          git add .
          git commit -m "chore(release): bump version to ${{ steps.version.outputs.new_version }}"
          
          # Пушим ветку
          git push origin "$BRANCH_NAME"
          
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
          
      - name: Generate changelog
        id: changelog
        run: |
          echo "📋 Генерируем changelog..."
          
          # Получаем коммиты для changelog
          LAST_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COMMITS=$(git log $LAST_TAG..HEAD~1 --oneline --grep="^feat\|^fix\|^perf\|^refactor" --perl-regexp)
          else
            COMMITS=$(git log --oneline --grep="^feat\|^fix\|^perf\|^refactor" --perl-regexp | head -20)
          fi
          
          CHANGELOG="## 🚀 Release v${{ steps.version.outputs.new_version }}\n\n"
          
          if [ -n "${{ github.event.inputs.release_notes }}" ]; then
            CHANGELOG="${CHANGELOG}${{ github.event.inputs.release_notes }}\n\n"
          fi
          
          if [ -n "$COMMITS" ]; then
            CHANGELOG="${CHANGELOG}### 📋 Изменения:\n\n"
            
            # Группируем коммиты по типам
            FEATURES=$(echo "$COMMITS" | grep "feat:" || true)
            FIXES=$(echo "$COMMITS" | grep "fix:" || true)
            PERF=$(echo "$COMMITS" | grep "perf:" || true)
            REFACTOR=$(echo "$COMMITS" | grep "refactor:" || true)
            
            if [ -n "$FEATURES" ]; then
              CHANGELOG="${CHANGELOG}#### ✨ Новые фичи:\n"
              echo "$FEATURES" | while read -r line; do
                MSG=$(echo "$line" | sed 's/^[a-f0-9]* //')
                CHANGELOG="${CHANGELOG}- $MSG\n"
              done
              CHANGELOG="${CHANGELOG}\n"
            fi
            
            if [ -n "$FIXES" ]; then
              CHANGELOG="${CHANGELOG}#### 🐛 Исправления:\n"
              echo "$FIXES" | while read -r line; do
                MSG=$(echo "$line" | sed 's/^[a-f0-9]* //')
                CHANGELOG="${CHANGELOG}- $MSG\n"
              done
              CHANGELOG="${CHANGELOG}\n"
            fi
            
            if [ -n "$PERF" ]; then
              CHANGELOG="${CHANGELOG}#### ⚡ Производительность:\n"
              echo "$PERF" | while read -r line; do
                MSG=$(echo "$line" | sed 's/^[a-f0-9]* //')
                CHANGELOG="${CHANGELOG}- $MSG\n"
              done
              CHANGELOG="${CHANGELOG}\n"
            fi
            
            if [ -n "$REFACTOR" ]; then
              CHANGELOG="${CHANGELOG}#### 🔧 Рефакторинг:\n"
              echo "$REFACTOR" | while read -r line; do
                MSG=$(echo "$line" | sed 's/^[a-f0-9]* //')
                CHANGELOG="${CHANGELOG}- $MSG\n"
              done
              CHANGELOG="${CHANGELOG}\n"
            fi
          else
            CHANGELOG="${CHANGELOG}### 📋 Изменения:\n- Обновления и улучшения\n\n"
          fi
          
          CHANGELOG="${CHANGELOG}---\n\n"
          CHANGELOG="${CHANGELOG}**Тип релиза:** ${{ steps.version.outputs.version_type }}\n"
          CHANGELOG="${CHANGELOG}**Предыдущая версия:** ${{ steps.version.outputs.current_version }}\n"
          CHANGELOG="${CHANGELOG}**Новая версия:** ${{ steps.version.outputs.new_version }}\n\n"
          CHANGELOG="${CHANGELOG}> ⚠️ **Это draft релиз.** Он станет полноценным после мержа release ветки в master."
          
          # Сохраняем changelog в файл для использования в PR и Release
          echo -e "$CHANGELOG" > /tmp/changelog.md
          
          # Экранируем для GitHub Output
          CHANGELOG_ESCAPED=$(echo -e "$CHANGELOG" | sed ':a;N;$!ba;s/\n/\\n/g' | sed 's/"/\\"/g')
          echo "changelog=$CHANGELOG_ESCAPED" >> $GITHUB_OUTPUT

      - name: Create Draft GitHub Release
        run: |
          echo "📝 Создаем draft GitHub Release..."
          
          gh release create "v${{ steps.version.outputs.new_version }}" \
            --title "🚀 Release v${{ steps.version.outputs.new_version }}" \
            --notes-file /tmp/changelog.md \
            --draft \
            --prerelease=${{ contains(steps.version.outputs.new_version, '-') }}
            
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Create Pull Request
        run: |
          echo "🔄 Создаем Pull Request..."
          
          # Создаем специальный changelog для PR
          PR_CHANGELOG="## 🚀 Release v${{ steps.version.outputs.new_version }}\n\n"
          PR_CHANGELOG="${PR_CHANGELOG}### 🎯 Что включено в релиз:\n\n"
          
          if [ -n "${{ github.event.inputs.release_notes }}" ]; then
            PR_CHANGELOG="${PR_CHANGELOG}${{ github.event.inputs.release_notes }}\n\n"
          fi
          
          # Добавляем информацию о теге и draft release
          PR_CHANGELOG="${PR_CHANGELOG}### 📋 Подготовка релиза:\n\n"
          PR_CHANGELOG="${PR_CHANGELOG}- ✅ **Тег создан:** \`v${{ steps.version.outputs.new_version }}\`\n"
          PR_CHANGELOG="${PR_CHANGELOG}- ✅ **Draft Release создан:** [v${{ steps.version.outputs.new_version }}](https://github.com/${{ github.repository }}/releases/tag/v${{ steps.version.outputs.new_version }})\n"
          PR_CHANGELOG="${PR_CHANGELOG}- ✅ **Версии обновлены** во всех пакетах\n\n"
          
          PR_CHANGELOG="${PR_CHANGELOG}### 🚀 После мержа:\n\n"
          PR_CHANGELOG="${PR_CHANGELOG}- 🔄 **Deploy workflow** запустится автоматически\n"
          PR_CHANGELOG="${PR_CHANGELOG}- 📦 **Пакеты** будут опубликованы в npm\n"
          PR_CHANGELOG="${PR_CHANGELOG}- 🎉 **Draft Release** станет полноценным релизом\n\n"
          
          PR_CHANGELOG="${PR_CHANGELOG}---\n\n"
          PR_CHANGELOG="${PR_CHANGELOG}**Тип релиза:** ${{ steps.version.outputs.version_type }}\n"
          PR_CHANGELOG="${PR_CHANGELOG}**Предыдущая версия:** ${{ steps.version.outputs.current_version }}\n"
          PR_CHANGELOG="${PR_CHANGELOG}**Новая версия:** ${{ steps.version.outputs.new_version }}"
          
          echo -e "$PR_CHANGELOG" > /tmp/pr_changelog.md
          
          gh pr create \
            --title "🚀 Release v${{ steps.version.outputs.new_version }}" \
            --body-file /tmp/pr_changelog.md \
            --label "release" \
            --label "${{ steps.version.outputs.version_type }}" \
            --assignee "${{ github.actor }}"
            
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Summary
        run: |
          echo "## 🎉 Release подготовлен!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Что создано:" >> $GITHUB_STEP_SUMMARY
          echo "- **🏷️ Тег:** \`v${{ steps.version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **📝 Draft Release:** [v${{ steps.version.outputs.new_version }}](https://github.com/${{ github.repository }}/releases/tag/v${{ steps.version.outputs.new_version }})" >> $GITHUB_STEP_SUMMARY
          echo "- **🌿 Release ветка:** \`release/v${{ steps.version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **🔄 Pull Request:** Создан автоматически" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 Детали релиза:" >> $GITHUB_STEP_SUMMARY
          echo "- **Версия:** \`${{ steps.version.outputs.current_version }}\` → \`${{ steps.version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Тип:** \`${{ steps.version.outputs.version_type }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Следующие шаги:" >> $GITHUB_STEP_SUMMARY
          echo "1. 👀 Проверьте созданный Pull Request" >> $GITHUB_STEP_SUMMARY
          echo "2. ⏳ Дождитесь прохождения CI проверок" >> $GITHUB_STEP_SUMMARY
          echo "3. 🔍 Сделайте code review" >> $GITHUB_STEP_SUMMARY
          echo "4. ✅ Смержите PR для автоматического деплоя" >> $GITHUB_STEP_SUMMARY
          echo "5. 🎉 Draft Release автоматически станет полноценным!" >> $GITHUB_STEP_SUMMARY 